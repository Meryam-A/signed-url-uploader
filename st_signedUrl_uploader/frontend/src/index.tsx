import React from "react"
import ReactDOM from "react-dom"
import SignedUrlUploader from "./SignedUrlUploader"

ReactDOM.render(
  <React.StrictMode>
    <SignedUrlUploader />
  </React.StrictMode>,
  document.getElementById("root")
)
